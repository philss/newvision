<%= form_for(@article) do |f| %>
  <% if @article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
      <% @article.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Título" %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label "Id do autor" %><br />
    <%= f.text_field :author_id %>
  </div>
  <div class="field">
    <%= f.label "Corpo do artigo" %><br />
    <%= f.text_area :body %>
  </div>
  <div class="actions">
    <%= f.submit "Salvar" %>
  </div>
<% end %>
